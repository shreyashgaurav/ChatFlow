#Heree we are "ignoring" the node modules(BIG)
#Recreatable with npm install
node_modules/
backend/node_modules/
frontend/node_modules/

# File containing passwords, secrets, API keys, DB URLs
.env
backend/.env
frontend/.env


# Temporaray Debug logs that are created when errors occurs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Specisfic files
# macOs
.DS_Store
# Windows 
Thumbs.db
# Vim editor
*.swp

# Build outputs Folders
# recreatable with npm run build
dist/
build/
.next/
out/

